import path from "node:path";

export const APP_PATH_REPO_ROOT = path.join(__dirname, "../..");

export const APP_PATH_REPO_SRC = path.join(APP_PATH_REPO_ROOT, "src");

export const APP_PATH_REPO_SRC_STATIC = path.join(APP_PATH_REPO_SRC, "static");
