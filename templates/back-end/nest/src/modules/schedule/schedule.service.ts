import { Injectable } from '@nestjs/common';

@Injectable()
export class ScheduleService {
  constructor(
  ) {
    //Código que estava aqui foi apagado pois referenciava o fila.service
    //e estava bugando na hora de subir a api tendo em vista que não passava 
    //parâmetros necessários para as funções

    //Buscar nos commits antigos para ver o código que estava aqui
  }
}
